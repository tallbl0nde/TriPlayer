#ifndef UTILS_RANDOM_HPP
#define UTILS_RANDOM_HPP

namespace Utils::Random {
    // Return a randomly generated number in the specified range
    size_t getSizeT(const size_t, const size_t);
}

#endif