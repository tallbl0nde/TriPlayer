#ifndef TYPES_HPP
#define TYPES_HPP

enum class RepeatMode {
    Off,        // Don't repeat
    One,        // Repeat the same song
    All         // Repeat the queue
};

typedef int SongID;

#endif